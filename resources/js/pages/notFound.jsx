import AppLayout from '../layouts/app-layout.jsx';
const notFound = () => {
    return (
        <AppLayout>
            <div className={'mx-4 flex items-center justify-between sm:container sm:mx-auto'}></div>
        </AppLayout>
    );
};

export default notFound;
